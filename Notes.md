# Project Notes

## Authentication

Login --> token(server) --> send this token to client(browser : localStorage, cookies)

- then we can do anything like add, delete, create --> req + token --> on server side check for token --> if not found -->
